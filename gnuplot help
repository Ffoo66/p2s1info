official doc : http://www.gnuplot.info/docs_5.4/Gnuplot_5_4.pdf
demos : http://www.gnuplot.info/demo/
more helpful site : http://www.gnuplotting.org/
re: about historigrams : http://www.gnuplotting.org/manpage-gnuplot-4-6/#Q1-1-147

Errorbars
on each line of the data file — (x, y, xlow, xhigh, ylow, yhigh).
see filledcurves, historigrams, errorbars

Voir using dans la doc

csv files
Syntax:
set datafile separator {whitespace | tab | comma | "chars"}
"csv" is short for "comma-separated values". The term "csv file" is loosely applied to files in which data
fields are delimited by a specific character, not necessarily a comma. To read data from a csv file you must
tell gnuplot what the field-delimiting character is. For instance to read from a file using semicolon as a field
delimiter:
set datafile separator ";"
See set datafile separator (p. 147). This applies only to files used for input. To create a csv file on
output, use the corresponding separator option to set table.

Set datafile missing
Syntax:
set datafile missing "<string>"
set datafile missing NaN
show datafile missing
unset datafile
The set datafile missing command tells gnuplot there is a special string used in input data files to denote
a missing data entry. There is no default character for missing. Gnuplot makes a distinction between
missing data and invalid data (e.g. "NaN", 1/0.). For example invalid data causes a gap in a line drawn
through sequential data points; missing data does not.
Non-numeric characters found in a numeric field will usually be interpreted as invalid rather than as a missing
data point unless they happen to match the missing string.
Conversely set datafile missing NaN causes all data or expressions evaluating to not-a-number (NaN) to
be treated as missing data.

Key The layout of certain plot styles (column-stacked histograms, spider plots) is such that it would make
no sense to generate plot titles from a data column header. Also it would make no sense to generate axis tic
labels from the content of a data column (e.g. using 2:3:xticlabels(1)). These plots styles instead use the
form using 2:3:key(1) to generate plot titles for the key from the text content of a data column, usually a
first column of row headers



use -persist option with gnuplot to have the plots persist past the main program's end.

gnuplot -persist <<-EOFMarker
	
EOFMarker
pause -1 "Hit any key to continue"


set output {"<filename>"}

for FILE in *
do
    gnuplot <<- EOF
        set xlabel "Label"
        set ylabel "Label2"
        set title "Graph title"   
        set term png
        set output "${FILE}.png"
        plot "${FILE}" using 1:2:3:4 with errorbars
EOF
done


shell help:
use AWK (see man awk for help) 
grep ";1;" data.csv pour avoir ttes les 1ères étapes de chaque trajet


horizontal : Boxxyerror
input : x y xlow xhigh ylow yhigh
boxes from x to xhigh >> x=0; xhigh=value
           y to yhigh >> y=position of the box on the y axis; yhigh=position + width 
histogramme :
set boxwidth 0.9 relative   
set style data histograms
histogramme regroupé:
set style histogram cluster
plot for [COL=2:4:2] 'file.dat' using COL
This will produce a plot with clusters of two boxes (vertical bars) centered at each integral value on the
x axis. If the first column of the input file contains labels, they may be placed along the x-axis using the
variant command
plot for [COL=2:4:2] 'file.dat' using COL:xticlabels(1)

-d1
histogramme horizontal
y: noms conducteurs
x: nb de trajets effectués

-d2
histogramme horizontal
y: noms conducteurs
x: distance totale parcourue

-l
histogramme vertical
x:id de trajet
y: distance

-t
histogramme vertical regroupé  >> set style histogram clustered
x:villes
y1:nb trajets
y2:nb départs
abscisse : noms des villes
ordonnée : nb entiers de 0 à max(y1,y2)


-s
graphique de type courbes min-max-moyenne >> Errorbars (see above)
3 courbes:
min(x)
max(x)
moy(x)
x: id de trajet
y:distance en km

// Option -s // 
//Format de results.dat//

x y ylow yhigh
1 5 1 7
2 10 4 14 
3 13 7 18
4 8 2 9
5 5 2 6 

avec    x: ID de trajet
	y: moy des distances 
	ylow: distance min
	yhigh: distance max





